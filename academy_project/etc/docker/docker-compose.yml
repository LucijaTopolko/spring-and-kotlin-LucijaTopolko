version: "3"

services:
  imdb-db-container:
    image: "postgres:13-alpine"
    ports:
      - "5433:5432"
    environment:
      - "POSTGRES_DB=checkupsdb"
      - "POSTGRES_USER=postgres"
      - "POSTGRES_PASSWORD=admin"
